services:
  - type: web
    name: yt-api
    runtime: node
    plan: free
    buildCommand: |
  export TERM=dumb
  pip3 install yt-dlp
  npm install
    startCommand: node server.js
    envVars:
      - key: POT_SERVER_URL
        value: https://bgutil-ytdlp-pot-provider-5vse.onrender.com
